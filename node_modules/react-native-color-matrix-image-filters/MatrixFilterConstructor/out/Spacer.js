import CurriedLambda from "./fable-core/CurriedLambda";
import fast_memoize from "fast-memoize";
import { Props } from "./fable/Fable.Helpers.ReactNative";
import { ofArray } from "./fable-core/List";
import { createElement } from "react";
import { View } from "react-native";
import { createObj } from "./fable-core/Util";
const containerStyle = CurriedLambda(fast_memoize(function (height) {
  return new Props.ViewProperties(8, ofArray([new Props.FlexStyle(20, height)]));
}));
export const view = createElement(View, createObj(ofArray([containerStyle(5)]), 1));