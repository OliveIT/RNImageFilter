import * as React from 'react';
import { Image } from 'react-native';
import {
    Normal,
    RGBA,
    Saturate,
    HueRotate,
    LuminanceToAlpha,
    Invert,
    Grayscale,
    Sepia,
    Nightvision,
    Warm,
    Cool,
    Brightness,
    Contrast,
    Temperature,
    Tint,
    Threshold,
    Technicolor,
    Polaroid,
    ToBGR,
    Kodachrome,
    Browni,
    Vintage,
    Night,
    Predator,
    Lsd,
    ColorTone,
    DuoTone,
    Protanomaly,
    Deuteranomaly,
    Tritanomaly,
    Protanopia,
    Deuteranopia,
    Tritanopia,
    Achromatopsia,
    Achromatomaly
} from 'react-native-color-matrix-image-filters';
  /*
export const OriginImage = (imageProps) => (
    <Image {...imageProps} />
);

export const GrayscaledImage = (imageProps) => (
    <Grayscale>
        <Image {...imageProps} />
    </Grayscale>
);

export const SepiaFilterImage = (imageProps) => (
    <Sepia>
        <Image {...imageProps} />
    </Sepia>
);

export const HueRotateImage = (imageProps) => (
<HueRotate>
    <Image {...imageProps} />
</HueRotate>
);

export const LuminanceToAlphaImage = (imageProps) => (
<LuminanceToAlpha>
    <Image {...imageProps} />
</LuminanceToAlpha>
);

export const HueRotateImage = (imageProps) => (
<HueRotate>
    <Image {...imageProps} />
</HueRotate>
);

export const SaturateImage = (imageProps) => (
<ColorMatrix
    matrix={concatColorMatrices([saturate(-0.9)])}
>
    <Image {...imageProps} />
</ColorMatrix>
);

export const ContrastImage = (imageProps) => (
<ColorMatrix
    matrix={concatColorMatrices([contrast(5.2)])}
    // alt: matrix={[saturate(-0.9), contrast(5.2), invert()]}
>
    <Image {...imageProps} />
</ColorMatrix>
);

export const InvertImage = (imageProps) => (
<ColorMatrix
    matrix={concatColorMatrices([invert()])}
    // alt: matrix={[saturate(-0.9), contrast(5.2), invert()]}
>
    <Image {...imageProps} />
</ColorMatrix>
);

export const ColorMatrixImage = (imageProps) => (
<ColorMatrix
    matrix={concatColorMatrices([saturate(-0.9), contrast(5.2), invert()])}
    // alt: matrix={[saturate(-0.9), contrast(5.2), invert()]}
>
    <Image {...imageProps} />
</ColorMatrix>
);*/